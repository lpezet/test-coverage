{
  "name": "myapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test-one": "nyc --check-coverage=false --reporter=html mocha",
    "test": "nyc --reporter=html --reporter=text-summary mocha --check-leaks --reporter spec \"./test/**/*.spec.ts\"",
    "tsc": "tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tsconfig-paths": "^4.0.0",
    "typescript": "^4.7.4"
  },
  "devDependencies": {
    "@types/mocha": "^9.1.1",
    "@types/node": "^18.6.0",
    "mocha": "^10.0.0",
    "nyc": "^15.1.0",
    "ts-node": "^10.9.1"
  }
}
